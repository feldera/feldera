
### Requirements

* kind: https://kind.sigs.k8s.io/
* kubectl
* docker
* Helm: https://helm.sh/docs/intro/install/

### Instructions

First, set up a kind cluster, the required Helm charts and the demo pod docker container with this script:

```
$: ./setup.sh
```

Next, bring up the DBSP namespace, manager, service and ingress rules:

```
$: kubectl apply -f dbsp-deploy.yml
```
