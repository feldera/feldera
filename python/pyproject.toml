[tool.poetry]
name = "dbsp"
version = "0.1.0"
description = "Database Stream Processor (DBSP) API"

authors = []

readme = "README.md"
packages = [{include = "dbsp"}]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.dependencies]
python = "^3.7"
dbsp-api-client = ">=0.1.0"
typing = ">=3.7"
pyyaml = ">=6.0"